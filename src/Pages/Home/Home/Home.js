import { Button } from '@mui/material';
import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>This is home</h1>
            <Button variant="contained">Hello WOrld</Button>
        </div>
    );
};

export default Home;